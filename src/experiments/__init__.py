"""One module per experiment. Each writes reports/<exp_id>/ with metrics, a
MANIFEST.json for reproducibility and an EVIDENCE.md stating what was measured,
derived, external, simulated and assumed."""
